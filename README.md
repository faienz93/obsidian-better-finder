# Obsidian Sample Plugin

```
node --version
v22.13.1
```

- open vscode project from `<YOUR_VALUT>/.obsidian/plugins/obsidian-better-finder`
- set hotkey
- set view to display only result

- search tag, when select a tag then show which document contains this tag
- same logic for other search method
